{% extends 'base.html.twig' %}

{% block title %}InstrumentSpecifique{% endblock %}

{% block body %}
    <h1>InstrumentSpecifique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instrument_specifique.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instrument_specifique_index') }}">back to list</a>

    <a href="{{ path('instrument_specifique_edit', {'id': instrument_specifique.id}) }}">edit</a>

    {{ include('instrument_specifique/_delete_form.html.twig') }}
{% endblock %}
